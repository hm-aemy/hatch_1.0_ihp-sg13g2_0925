image:
  name: ghcr.io/hm-aemy/iic-osic-tools:latest
  entrypoint: [""]

before_script:
  - sudo ./scripts/prepare-env.sh

build:
  stage: build
  script:
    - nix-shell --pure /opt/librelane --run "librelane --version"
